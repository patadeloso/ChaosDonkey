# Claude Notes

## Project Context
This repository is a Magento 2 module (`shaunmcmanus/chaosdonkey`), not a full Magento application. Use this file as a short Claude/Harness orientation only, and follow `AGENTS.md` as the authoritative source for the repository's detailed structure, coding, testing, and PR rules.

## Useful Commands
- `composer validate`
- `vendor/bin/phpunit`

## Magento CLI Limitation
- Commands such as `bin/magento chaosdonkey:status` and `bin/magento chaosdonkey:kick` must be run from a Magento installation root where this module is installed.
